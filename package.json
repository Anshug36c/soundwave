{
  "name": "soundwave",
  "version": "1.0.0",
  "description": "SoundWave — Spotify-like music streaming PWA",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm run build:client",
    "build:client": "test -x client/node_modules/.bin/vite || npm --prefix client ci --include=dev --no-audit --no-fund || npm --prefix client install --include=dev --no-audit --no-fund; npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
