# Pin npm as the package manager for this monorepo. Without a root lockfile some
# platforms (Vercel) default to bun, which installs only the root package's own
# dependency and never touches client/node_modules — see vercel.json.
engine-strict=true
