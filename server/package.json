{
  "name": "soundwave-server",
  "version": "1.0.0",
  "description": "SoundWave backend — unified music API aggregator",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node --openssl-legacy-provider server.js",
    "dev": "node --openssl-legacy-provider --watch server.js",
    "test": "node --test test/auth.test.js"
  },
  "dependencies": {
    "compression": "^1.8.2",
    "express": "^4.21.2"
  }
}
